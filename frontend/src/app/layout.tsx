import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "EcoTrace AI — Carbon Footprint Tracking & Awareness Platform",
  description: "Track, analyze, and reduce your carbon footprint through AI-powered utility bill parsing. Upload electricity, gas, and water bills to get personalized insights and reduction action plans.",
  keywords: ["carbon footprint", "sustainability", "climate", "emissions tracker", "eco", "carbon calculator"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
